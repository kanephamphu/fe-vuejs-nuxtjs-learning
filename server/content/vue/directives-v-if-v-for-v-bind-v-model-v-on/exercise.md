# Exercise: Directive Specialist

- [x] I know the shorthand for `v-bind` is `:`.
- [x] I know the shorthand for `v-on` is `@`.

? Which directive is used for conditional rendering? ! v-if
? Which directive is used for two-way data binding? ! v-model
? Which directive is used to render a list of items? ! v-for

? Vue directives (Multiple Choice):
- [X] v-if
- [X] v-for
- [X] v-bind
- [X] v-model
- [ ] v-style (it's :style, using v-bind)

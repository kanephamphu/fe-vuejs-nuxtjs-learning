# Exercise: Event Emitter

- [x] I understand that `$emit` sends events up the component tree.
- [x] I know how to pass data along with an emitted event.

? Which method is used to trigger an event from a child component? ! $emit
? Which directive does the parent use to listen to a child's event? ! v-on
? Can you pass multiple arguments with `$emit`? ! Yes

? Emits best practices (Multiple Choice):
- [X] Declare emits in the `emits` option
- [X] Use kebab-case for event names in templates
- [ ] Use props to change parent state directly

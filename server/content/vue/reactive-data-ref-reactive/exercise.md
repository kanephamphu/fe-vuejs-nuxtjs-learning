# Exercise: Reactivity Expert

- [x] I understand when to use `ref` vs `reactive`.
- [x] I know that `ref` requires `.value` in script blocks.

? Which function is typically used for primitive values like strings or numbers? ! ref
? Do you need to use `.value` to access a `ref` inside the `<template>`? ! No
? Can `reactive` be used with a primitive string value? ! No

? Reactive state check (Multiple Choice):
- [X] ref(0) is correct
- [X] reactive({count: 0}) is correct
- [ ] reactive(10) is correct
- [ ] ref.value inside template is required

# Exercise: Type Logic Pro

- [x] I understand how to use `typeof` on a variable.
- [x] I can extract keys using `keyof`.

? Which operator is used to get all keys of a type as a union? ! keyof
? Which operator is used to get the type of a value? ! typeof
? How do you access a property type? ! []

? Type logic results (Multiple Choice):
- [x] typeof "Hello" is string
- [x] keyof { a: 1 } is "a"
- [x] { a: string }["a"] is string
- [ ] typeof 10 is "number" (string literal type)

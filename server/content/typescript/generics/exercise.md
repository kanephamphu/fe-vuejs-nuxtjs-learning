# Exercise: Generic Master

- [x] I understand that `T` is a placeholder for a type.
- [x] I can use generics with functions and interfaces.

? In the function `function log<T>(val: T)`, what does `T` represent? ! A Type Parameter
? How do you call the identity function with a boolean type? ! identity<boolean>(true)
? Generics help improve code ______ by allowing types to be passed as arguments. ! reusability

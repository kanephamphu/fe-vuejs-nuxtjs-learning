# Exercise: Complex Types

- [x] I understand the difference between `type` and `interface`.
- [x] I can use the union operator `|`.

? Which operator is used for Union types? ! |
? Which operator is used for Intersection types? ! &
? Can a Type Alias be changed after it is defined? ! No

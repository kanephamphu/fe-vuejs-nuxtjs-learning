# Exercise: Route Ranger

- [x] I know that `[id].vue` creates a parameter named `id`.
- [x] I understand how to access parameters via `useRoute()`.

? If I have `pages/posts/[slug].vue`, which property of `route.params` should I use? ! slug
? Which filename syntax is used for a catch-all route? ! [...slug].vue
? What does `route.params` contain? ! URL parameters

? Dynamic routing examples (Multiple Choice):
- [X] [id].vue
- [X] [category]/[id].vue
- [X] [...all].vue
- [ ] {id}.vue

# Exercise: Secret Keeper

- [x] I know that `.env` stores environment-specific values.
- [x] I understand that `NUXT_` prefix overrides `runtimeConfig`.

? Which file is used to store local environment variables? ! .env
? Should you push your `.env` file to GitHub? ! No
? Which object in `runtimeConfig` is safe for browser-side keys? ! public

? Correct `.env` practice (Multiple Choice):
- [X] Add .env to .gitignore
- [X] Use NUXT_ prefix for overrides
- [X] Keep API secrets in private config
- [ ] Share .env file in Slack

# Exercise: Meta Master

- [x] I understand that `definePageMeta` is only for pages.
- [x] I know how to change a layout using this macro.

? Which property is used to specify a custom layout? ! layout
? Where must the page component be located to use `definePageMeta`? ! pages
? Which property is used to apply page-specific middleware? ! middleware

? definePageMeta options (Multiple Choice):
- [X] layout
- [X] middleware
- [X] alias
- [X] keepalive
- [ ] head (use useHead instead)

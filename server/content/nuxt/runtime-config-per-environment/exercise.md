# Exercise: Config Ninja

- [x] I understand the difference between public and private config.
- [x] I know how to access config via `useRuntimeConfig()`.

? Which property in `runtimeConfig` is exposed to the browser? ! public
? Which composable is used to access the runtime configuration? ! useRuntimeConfig
? Environment variables overriding runtime config must start with which prefix? ! NUXT_

? Runtime config characteristics (Multiple Choice):
- [X] Public keys are client-side
- [X] Private keys are server-side only
- [X] Overridable via .env
- [ ] Values are baked in at compile-time only

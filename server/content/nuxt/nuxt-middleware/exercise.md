# Exercise: Middleware Master

- [x] I know that `middleware/` is the directory for named middleware.
- [x] I understand that `navigateTo` is used for redirection.

? Which directory hosts global and named middleware? ! middleware
? What suffix do global middleware files use? ! .global
? Which function is used to redirect a user within middleware? ! navigateTo

? Middleware types (Multiple Choice):
- [X] Global
- [X] Named
- [X] Anonymous (Inline)
- [ ] Server-only (that's server middleware/plugins)
